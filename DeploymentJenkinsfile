pipeline {
  agent any
  environment {
    GH_TOKEN = credentials("gh-pat")
  }
  stages {
    stage("Install") {
      steps {
        sh "npm ci"
      }
    }
    stage("Build") {
      steps {
        sh "npm run build"
      }
    }
  }
}